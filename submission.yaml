# =============================================================================
# HACKATHON SUBMISSION METADATA
# =============================================================================
# Instructions:
#   - Fill in ALL required fields (marked with # REQUIRED)
#   - Optional fields can be left as empty strings ""
#   - Do NOT rename this file — the evaluation pipeline reads it by name
#   - Use double quotes around all string values
# =============================================================================

team:
  name: "Telemetry Titans"                             # REQUIRED — e.g., "Orion Squad"
  track: "AI"                            # REQUIRED — one of: AI | DevOps | Sustainability | Open
  lead:
    name: "Vedant Patel"                           # REQUIRED — e.g., "Alice Chen"
    email: "24dcs092@charusat.edu.in"                          # REQUIRED — e.g., "alice.chen@ibm.com"
  members:
    - name: "Het Talpara"                         # REQUIRED — Team member 1
      email: "24dcs132@charusat.edu.in"
    - name: "Parth Thakkar"                         # Add more members by copying the two lines above
      email: "24dcs135@charusat.edu.in"
    - name: "Yug Umrania"
      email: "24dcs140@charusat.edu.in"

submission:
  title: "GridHealth AI"                            # REQUIRED — Project display name, e.g., "SmartOps Dashboard"

  problem_statement: >                 # REQUIRED — 1-3 sentences. What problem? Who has it?
    "Power transformer and substation failures cause blackouts costing utilities $1M+/hour and affecting millions of people. Most utilities still use calendar-based maintenance, while sensors already measuring temperature, vibration, partial discharge, and oil quality show failure signatures weeks in advance. Weather events compound the risk — but sensor data and weather forecasts are never combined in time to act."

  solution_summary: >                  # REQUIRED — 1-3 sentences. What did you build?
    "GridHealth AI combines transformer/substation sensor health data, weather forecasts, and historical incident records into risk-ranked predictions. It surfaces outage-prone assets on a live map, ranks equipment by grid-impact severity, and generates a prioritized maintenance and crew pre-positioning plan."

  key_features:                        # REQUIRED — List 3 to 5 specific features you implemented
    - "Failure prediction model (XGBoost) scoring asset failure probability from sensor trends"
    - "Risk-ranking formula weighing sensor alarms, weather exposure, and customers served"
    - "Interactive grid map with risk-colored asset markers (Leaflet)"
    - "Prioritized maintenance and crew pre-positioning plan generator"
    - "Asset detail view with sensor trend charts and SHAP explanation"

  tech_stack:
    languages: ["Python", "Typescript", "Javascript"]                      # REQUIRED — e.g., ["Python", "TypeScript"]
    frameworks: ["React", "FastAPI", "SHAP", "XGBoost"]                     # e.g., ["FastAPI", "React"]
    ibm_technologies: ["IBM Bob"]               # e.g., ["watsonx.ai", "IBM Bob", "IBM Cloud Functions"]
    databases: ["SQLite"]                      # e.g., ["PostgreSQL", "Redis"]
    other: ["OpenWhetherMap API", "Nova", "Github Actions", "Git"]                          # e.g., ["Docker", "GitHub Actions", "Slack API"]

  what_we_are_most_proud_of: >         # Optional but recommended — guides judges to your best work
    The end-to-end data pipeline that generates scientifically credible synthetic sensor failure
    signatures (temperature drift, vibration spikes, partial discharge) using real transformer-failure
    physics, combined with an interactive React dashboard that surfaces risk-ranked assets on a live
    Leaflet map with SHAP-explained predictions. The tight separation between data generation, SQLite
    database, and frontend means the system is ready to replace mock data with real SCADA feeds with
    minimal changes.

  known_limitations: >                 # Optional but recommended — be honest about gaps
    The FastAPI backend is planned but not yet wired up; the frontend currently runs on rich mock data.
    The XGBoost model scoring is embedded in the data-generation pipeline rather than as a deployed
    inference endpoint. Authentication is not implemented. The app has been tested on Chrome and Firefox
    only. Live weather integration requires a free OpenWeatherMap API key; synthetic weather is the default.

# =============================================================================
# ARTIFACT LOCATIONS
# These paths are relative to the repo root. Only change if you moved files.
# =============================================================================
artifacts:
  source_code: "src/"
  setup_guide: "docs/setup-guide.md"
  architecture_doc: "docs/architecture.md"
  demo_video: "demo/demo-video-link.txt"   # URL to hosted video (YouTube, Loom, Box)
  live_demo: "demo/live-demo-url.txt"      # URL to deployed demo (optional)
  screenshots: "demo/screenshots/"
  presentation: "presentation/"
